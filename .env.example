# Shown in contact sidebar and used as the mailto: recipient for the contact form.
PUBLIC_CONTACT_EMAIL=hello@willowhome.example

# Owner CMS at /cms — required in production (otherwise CMS is open without login).
CMS_ADMIN_PASSWORD=

# Optional: secret for signing the cms_session cookie (defaults to CMS_ADMIN_PASSWORD).
CMS_SESSION_SECRET=
